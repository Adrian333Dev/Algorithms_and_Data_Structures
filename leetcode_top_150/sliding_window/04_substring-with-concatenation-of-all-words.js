/**
 * @param {string} s
 * @param {string[]} words
 * @return {number[]}
 */
const findSubstring = (s, words) => {
	if (!words.length) return [];
  const subLen = words.length * words[0].length;
  
};
