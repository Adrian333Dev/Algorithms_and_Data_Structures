/**
 * @param {string} s
 * @param {number} k
 * @return {number}
 */
const characterReplacement = (s, k) => {
  let repo = {};
  for (let i = 0; i < s.length; i++) repo[s[i]] = (repo[s[i]] || 0) + 1;
  let l = r = max = 0;
  while (r < s.length) {
    
  }
};
