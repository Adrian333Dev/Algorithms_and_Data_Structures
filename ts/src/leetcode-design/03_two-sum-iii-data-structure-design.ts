class TwoSum {
  private set = new Set<number>();

  constructor() {}

  add(number: number): void {
    this.set.add(number);
  }

  find(value: number): boolean {
    return false;
  }
}
